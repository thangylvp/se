# dn_asr environment — conda + pip
#
#   conda create -n dn_asr python=3.11 -y && conda activate dn_asr
#   # GPU torch first (CUDA 12.8 build — what this repo is verified against; Blackwell/RTX 5090):
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# torch is installed separately (above) so you get the right CUDA build; for
# CPU-only, `pip install torch==2.8.0` works too. Versions below are the exact
# ones this repo was verified against.

# --- deep learning / ASR ---
transformers==4.51.3      # frozen Whisper (WhisperForConditionalGeneration / Processor)
einops==0.8.2             # tensor rearrange in DeepVQE

# --- audio I/O + DSP ---
librosa==0.11.0           # resampling, audio load
soundfile==0.13.1         # wav/flac read/write
numpy==1.26.4

# --- metrics / utils ---
jiwer==4.0.0              # WER
tqdm==4.67.3

# transformers pulls in tokenizers / huggingface-hub / safetensors automatically.
# A Hugging Face token (in .env as HF_TOKEN) is needed to download the Whisper model.
